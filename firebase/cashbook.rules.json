{
  "//": "TARGET rules for the CASHBOOK app (project recat-auth-test). DO NOT APPLY YET.",
  "//BLOCKER": "STILL DO NOT APPLY. Phase 1 (client-side) now writes /users/$shop/members/$uid on signup and claims it on first login, but existing shops are only mapped as their owners log in. Run scripts/audit-membership.mjs and set ENFORCE_MEMBERSHIP=true first. Applying these rules before every active shop is mapped locks those shops out.",
  "//UNRESOLVED": "Two known gaps in these rules: loginUser reads /users/$shop BEFORE signing in (unauthenticated, would be denied), and /feedback + /auditLogs are not covered here at all.",
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$shop": {
        ".read": "auth != null && data.child('members').child(auth.uid).exists()",
        ".write": "auth != null && (data.child('members').child(auth.uid).exists() || !data.exists())",

        "signupInfo": {
          "email": {
            ".read": true,
            "//": "public lookup so a member can find the shop before signing in"
          }
        }
      }
    },

    "$shop": {
      "//": "the ledger itself: {username}/{YYYY-MM-DD}/{in|out|liquid}, plus goals/reminders/recycleBin/counters",
      ".read": "auth != null && root.child('users').child($shop).child('members').child(auth.uid).exists()",
      ".write": "auth != null && root.child('users').child($shop).child('members').child(auth.uid).exists()"
    }
  }
}
